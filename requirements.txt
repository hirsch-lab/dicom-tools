# Install with pip:
#   python -m pip install -r requirements.txt
pydicom>=2.1            # DICOM data structure for python
pandas>=1.1             # Data analysis library
progressbar2            # To indicate progress
mypy>=0.700             # For static code analysis
pyyaml>=5               # For reading/parsing YAML files

dicom2nifti>=2.2        # For conversion DICOM -> NIfTI
Pillow>=8               # For image handling and conversions

opencv-python
coverage==5.5
pytest==6.2.1
pytest-cov==2.11.1
# coveralls==2.2.0
